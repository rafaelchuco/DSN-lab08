# TechStore - Frontend (scaffold)

Scaffold React (Vite) minimal para autenticación y listado de productos.

Instalación y ejecución:

```bash
cd client
npm install
npm run dev
```

El cliente asume backend en la misma origin (`/api/...`). En desarrollo con Vite, puedes configurar proxy si el backend corre en otro puerto.
